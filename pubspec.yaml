name: vcr
description: A package to mock requests in tests, storing request in json files and reusing
version: 0.1.1
homepage: https://github.com/keviinlouis/vcr

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mockito: ^4.1.1
  dio: ^3.0.4
  path_provider: 1.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
